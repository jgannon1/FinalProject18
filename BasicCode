import random

class Card:

    def __init__(self, suit, name):
        self.suit = suit
        self.name = name
        self.value = self.get_value(name)

    def get_value(self, name):
        values = {'ace': 1, 'two': 2, 'three': 3, 'four': 4, 'five': 5, 'six': 6, 'seven': 7, 'eight': 8, 'nine': 9, 'ten': 10, 'jack': 11, 'queen': 12, 'king': 13}
        return values[name]

    def __str__(self):
        return f"{self.name} of {self.suit}"

    __repr__ = __str__

deck = []

for suit in ['spades', 'diamonds', 'hearts', 'clubs']:
    for name in ['ace', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine', 'ten', 'jack', 'queen', 'king']:
        print(f"{name} of {suit}")
        deck.append(Card(suit, name))

print(deck)
for card in deck:
    print(card.name, card.value)

def evaluate_winner(dealer_total, player_total):
    """Determines a winner between the dealer and player"""
    if dealer_total >= player_total and < 21:
        print(f"House wins! Dealer has {dealer_total} and you have {player_total}")
    else:
        print(f"You win! Dealer has {dealer_total} and you have {player_total}")

def draw_card():
    NEED TO FIGURE OUT HOW TO DRAW A CARD FROM THE DECK DOES THE CLASS WORK THE SAME WAY A LIST DOES? (might have to change where we use the classes, i hate myself -_-)

def dealer_total():
    """Evaluates the dealer's hand"""
    draw_card() x2
    current_sum = card1 + card2
    if current_sum <= 14:
        print(f"Dealer currently has {current_sum}, dealer hits.")
        draw_Card()
    else:
        print(f"Dealer stay at {current_sum}.")
    return current_sum

def player_total():
        """Evaluates player's hand"""



